<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="PM02E2GRUPO2.Views.ListViewPage">
    <ContentPage.Resources>
        <ResourceDictionary>
            <!-- <Converters:ByteArrayToImageSourceConverter x:Key="ByteArrayToImage" /> -->
        </ResourceDictionary>
    </ContentPage.Resources>
    <ContentPage.Content>
        <StackLayout>
            <ListView x:Name="listview_ubicaciones" ItemTapped="listview_ubicaciones_ItemTapped">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <!-- <ImageCell ImageSource="{Binding Imagen, Converter={StaticResource ByteArrayToImage}}" Text="{Binding Descripcion}" 
                                   TextColor="Black"/> -->
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>